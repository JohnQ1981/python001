# Lists is one of the Data Structure
# list is like a container, but ordered collection of values
# we can put any data, it is a collection of values and it is ordered.
tasks =["Trash", "Dishes", "Laundry", "dinner"]
print(tasks)
for c in tasks:
    print(c)

num_elem= input("number of Elements you want to be in the list")
print(f"You entered {num_elem} , your list is")
for c in num_elem:
    list += input(list(range(0,int(num_elem)-1)))

waitlist =['tom','ali','veli']